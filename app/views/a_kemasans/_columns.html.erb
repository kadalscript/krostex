<% AKemasan.column_names.each do |column_name| %>
  <% unless @hidden_columns.include?(column_name) %>
    <%= raw(showing_row(AKemasan, column_name, attributes, f, @a_kemasan)) %>
  <% end %>
<% end %>