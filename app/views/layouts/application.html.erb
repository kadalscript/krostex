<!DOCTYPE html>
<html>
<head>
	<title>KROSTEX'S SYSTEM</title>
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<div class="container-fluid" style="margin-top:10px">
    <% if not action_name.eql? "index" %>
      <% if alert || notice %>
        <!-- message() helper is located in app/helpers/application_helper.rb -->
        <div class="alert <%= message(alert, notice) %> fade in">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <%= alert || notice %>
        </div>
        <!-- message() helper is located in app/helpers/application_helper.rb -->
      <% end %>
    <% end %>
		<%= yield %>
	</div>
</body>
</html>
