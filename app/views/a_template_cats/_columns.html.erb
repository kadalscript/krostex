<% ATemplateCat.column_names.each do |column_name| %>
  <% unless @hidden_columns.include?(column_name) %>
    <% if !(["new", "create"].include?(action_name) && ["updated_at", "updated_by"].include?(column_name)) %>
      <%= raw(showing_row(ATemplateCat, column_name, attributes, f, @a_template_cat)) %>
    <% end %>
  <% end %>
<% end %>
