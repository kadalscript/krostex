<tr>
  <th colspan="3">
    <%= link_to 'Tambah' , new_a_city_path  %>
    <%= link_to 'Koreksi', edit_a_city_path(@a_city) if @a_city.id %>
    <%= link_to 'Hapus'  , a_cities_destroy_show_path(@a_city) if action_name != 'destroy_show' %>
  </th>
</tr>